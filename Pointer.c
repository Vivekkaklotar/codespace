#include<stdio.h>

/* pointer is */
int main()
{
    // int a=10;
    // int *p=&a;

    // printf("\n a is %d \n",a);
    // printf("\n address of a is %p \n",&a);
    // printf("\n pointer p is %p \n",p);
    // printf("\n value stored in address of pointer %d \n",*p);

    // /* create a function with parameters to swap 2 numbers*/

    
    return 0;

    
}